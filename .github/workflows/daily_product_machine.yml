name: Ultimate_Verification_Final
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Create Product via Standard API
        run: |
          # यह सबसे सटीक तरीका है जो आपकी तस्वीरों के साथ मेल खाता है
          RESPONSE=$(curl -s -X POST "https://api.gumroad.com/v2/products" \
            -H "Authorization: Bearer ${{ secrets.GUMROAD_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d '{
              "name": "Machine_Victory_$(date +%H%M)",
              "price_cents": 1000,
              "description": "Final Expert Attempt",
              "published": true
            }')

          echo "Response: $RESPONSE"

          # टेलीग्राम पर साफ संदेश
          if [[ $RESPONSE == *"\"success\":true"* ]]; then
             MSG="✅ अविश्वसनीय! मशीन आखिरकार चालू हो गई।"
          elif [[ $RESPONSE == *"<!DOCTYPE html"* ]]; then
             MSG="❌ अभी भी 404 (Page Not Found) आ रहा है। यह कोडिंग की नहीं, अकाउंट की दिक्कत लग रही है।"
          else
             MSG="📊 जवाब: $RESPONSE"
          fi

          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage" \
          -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
          -d text="$MSG"
