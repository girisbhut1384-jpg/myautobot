name: Machine_Final_Success
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Create Product
        run: |
          # यह वही तरीका है जो आपके ब्राउज़र में चला
          RESPONSE=$(curl -s -X POST "https://api.gumroad.com/v2/products" \
            -d "access_token=${{ secrets.GUMROAD_TOKEN }}" \
            -d "name=Final_Success_Product_$(date +%H%M)" \
            -d "price=1000" \
            -d "description=Automated Success" \
            -d "published=true")

          echo "Response: $RESPONSE"

          # टेलीग्राम पर सीधा संदेश
          if [[ $RESPONSE == *"\"success\":true"* ]]; then
             MSG="✅ मुबारक हो! मशीन ने गमरोड के अंदर घुसकर प्रोडक्ट बना दिया है।"
          else
             MSG="📊 गमरोड का जवाब: $RESPONSE"
          fi

          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage" \
          -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
          -d text="$MSG"
