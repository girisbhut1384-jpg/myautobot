name: Super_Direct_Success
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Create Product
        run: |
          # हमने URL को बिल्कुल सरल कर दिया है जो हर हाल में काम करता है
          RESPONSE=$(curl -s -X POST "https://api.gumroad.com/v2/products" \
            --data-urlencode "access_token=${{ secrets.GUMROAD_TOKEN }}" \
            --data-urlencode "name=AI_Master_Success_$(date +%H%M)" \
            --data-urlencode "price=1000" \
            --data-urlencode "description=Automated Premium Guide" \
            --data-urlencode "published=true")

          echo "Full Response: $RESPONSE"

          # टेलीग्राम पर रिपोर्ट - अब बिना किसी झंझट के
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage" \
          -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
          -d text="🚀 *मशीन का ताज़ा जवाब:* %0A%0A$RESPONSE"
