name: Ultimate_Final_Fix_2026
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Create Product
        run: |
          # नया तरीका: access_token को बॉडी के अंदर भेजना
          RESPONSE=$(curl -s -X POST "https://api.gumroad.com/v2/products" \
            -d "access_token=${{ secrets.GUMROAD_TOKEN }}" \
            -d "name=Final_Success_Product_$(date +%H%M)" \
            -d "price=1000" \
            -d "description=Automated Premium Content" \
            -d "countries[]=IN" \
            -d "published=true")

          echo "Gumroad Response: $RESPONSE"

          # टेलीग्राम पर रिपोर्ट
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage" \
          -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
          -d text="📊 *मशीन की अंतिम रिपोर्ट:* %0A%0A$RESPONSE" \
          -d parse_mode="Markdown"
