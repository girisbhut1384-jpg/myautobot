name: Final_War_Victory_2026
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Create Product via Forced Parameter
        run: |
          # हमने टोकन को सीधा लिंक के अंदर डाल दिया है (असिस्टेंट के सुझाव के अनुसार)
          # यह 404 आने की गुंजाइश को खत्म कर देगा
          RESPONSE=$(curl -s -X POST "https://api.gumroad.com/v2/products?access_token=${{ secrets.GUMROAD_TOKEN }}" \
            -H "Content-Type: application/x-www-form-urlencoded" \
            -d "name=Final_Victory_$(date +%H%M)" \
            -d "price_cents=1000" \
            -d "description=Confirmed Machine Success" \
            -d "published=true")

          echo "Response: $RESPONSE"

          if [[ $RESPONSE == *"\"success\":true"* ]]; then
             MSG="✅ इतिहास गवाह है! आपकी मेहनत रंग लाई। प्रोडक्ट लाइव है।"
          else
             # अगर अभी भी कचरा आए, तो यह उसे साफ करके बताएगा
             MSG="📊 फाइनल रिपोर्ट: $(echo $RESPONSE | grep -o '\"message\":\"[^\"]*\"' || echo $RESPONSE | head -c 50)"
          fi

          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage" \
          -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
          -d text="$MSG"
