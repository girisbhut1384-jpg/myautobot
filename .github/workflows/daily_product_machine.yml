name: 100_Percent_Success_Final
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Create Product and Notify
        run: |
          # 1. गमरोड को सीधा और सही पते पर डेटा भेजना
          RESPONSE=$(curl -s -X POST "https://api.gumroad.com/v2/products" \
            -H "Authorization: Bearer ${{ secrets.GUMROAD_TOKEN }}" \
            -d "name=AI_Ebook_Master_$(date +%H%M)" \
            -d "price=1000" \
            -d "description=Premium Automation Guide" \
            -d "published=true")

          echo "Gumroad Response: $RESPONSE"

          # 2. टेलीग्राम को डेटा भेजना (बिना किसी देरी के)
          # अगर यहाँ गड़बड़ हुई तो GitHub Error दिखा देगा
          curl -f -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage" \
          -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
          -d text="📊 *असली रिपोर्ट:* %0A%0A$RESPONSE" \
          -d parse_mode="Markdown"
