name: 100_Percent_Final_Success
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Send Command to Gumroad
        run: |
          # हमने यहाँ से हर वो चीज़ हटा दी है जिससे एरर आ सकता था
          RESPONSE=$(curl -s -X POST https://api.gumroad.com/v2/products \
            -d "access_token=${{ secrets.GUMROAD_TOKEN }}" \
            -d "name=ULTIMATE_SUCCESS_$(date +%H%M)" \
            -d "price=1000" \
            -d "description=Official Automated Product" \
            -d "published=true")

          # टेलीग्राम पर सीधा कच्चा चिट्ठा भेजना
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage \
          -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
          -d text="📊 *Gumroad Report:* %0A%0A$RESPONSE" \
          -d parse_mode="Markdown"
