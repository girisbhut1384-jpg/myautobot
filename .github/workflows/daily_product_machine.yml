name: 100_Percent_Final_Success
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Create Product
        run: |
          # ध्यान दें: URL के अंत में / डालना ज़रूरी है
          RESPONSE=$(curl -s -X POST "https://api.gumroad.com/v2/products" \
            -H "Authorization: Bearer ${{ secrets.GUMROAD_TOKEN }}" \
            -d "name=AI_Ebook_Mastery_$(date +%H%M)" \
            -d "price=1000" \
            -d "description=Premium Automation Guide" \
            -d "published=true")

          echo "Response: $RESPONSE"

          # टेलीग्राम पर रिपोर्ट भेजना
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage \
          -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
          -d text="📊 *असली रिपोर्ट:* %0A%0A$RESPONSE" \
          -d parse_mode="Markdown"
