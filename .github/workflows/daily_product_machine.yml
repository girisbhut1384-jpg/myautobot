name: Ultimate_Force_Final
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Create Product via Direct Endpoint
        run: |
          # हमने api. शब्द को हटा दिया है और पुराने लिंक का उपयोग किया है
          # यह वह आखिरी रास्ता है जो 404 को बाईपास कर सकता है
          RESPONSE=$(curl -s -X POST "https://gumroad.com/api/v2/products" \
            -d "access_token=${{ secrets.GUMROAD_TOKEN }}" \
            -d "name=Final_Victory_$(date +%H%M)" \
            -d "price=1000" \
            -d "description=Automated Success" \
            -d "published=true")

          echo "Response: $RESPONSE"

          if [[ $RESPONSE == *"\"success\":true"* ]]; then
             MSG="✅ अविश्वसनीय! आखिरकार सही रास्ता मिल गया। चेक करें!"
          else
             # अगर अभी भी 404 आए, तो हमें असिस्टेंट को ये सबूत भेजना होगा
             CLEAN=$(echo $RESPONSE | head -c 100)
             MSG="📊 रिपोर्ट: $CLEAN"
          fi

          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage" \
          -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
          -d text="$MSG"
