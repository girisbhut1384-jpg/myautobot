name: 100% Auto Product Machine
on:
  workflow_dispatch:

jobs:
  build_and_publish:
    runs-on: ubuntu-latest
    steps:
      - name: Create Product
        run: |
          curl -s -X POST https://api.gumroad.com/v2/products \
            -d "access_token=${{ secrets.GUMROAD_TOKEN }}" \
            -d "name=AI_Mastery_Success_$(date +%H%M)" \
            -d "price=1000" \
            -d "description=Official Automated Product" \
            -d "published=true"
          
      - name: Notify Telegram
        run: |
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage \
          -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
          -d text="🚀 *बधाई हो!* Gumroad पर प्रोडक्ट सफलतापूर्वक भेज दिया गया है।" \
          -d parse_mode="Markdown"
