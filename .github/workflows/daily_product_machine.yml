name: Ultimate_Gumroad_Success
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Create Product
        run: |
          # हमने URL को बिल्कुल नया और सीधा कर दिया है
          # ध्यान दें: /v2 हटा दिया गया है
          RESPONSE=$(curl -s -X POST "https://api.gumroad.com/products" \
            -d "access_token=${{ secrets.GUMROAD_TOKEN }}" \
            -d "name=AI_Ebook_Final_$(date +%H%M)" \
            -d "price=1000" \
            -d "description=Master Automation Guide" \
            -d "published=true")

          echo "Response: $RESPONSE"

          # टेलीग्राम पर सिर्फ काम की रिपोर्ट
          if [[ $RESPONSE == *"success\":true"* ]]; then
             MSG="✅ धमाका! प्रोडक्ट गमरोड पर लाइव हो चुका है।"
          else
             MSG="📊 गमरोड का नया जवाब: $RESPONSE"
          fi

          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage" \
          -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
          -d text="$MSG"
