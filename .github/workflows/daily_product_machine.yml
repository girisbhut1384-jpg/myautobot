name: Final_Success_Machine
on:
  workflow_dispatch:

jobs:
  launch:
    runs-on: ubuntu-latest
    steps:
      - name: Force Publish Product
        run: |
          # बिना किसी तामझाम के सीधा कमांड
          curl -X POST https://api.gumroad.com/v2/products \
            -d "access_token=${{ secrets.GUMROAD_TOKEN }}" \
            -d "name=AI_Final_Success_$(date +%H%M)" \
            -d "price=1000" \
            -d "description=Official Automated Digital Product" \
            -d "published=true"
          
      - name: Telegram Report
        run: |
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage \
          -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
          -d text="🚀 *मिशन सफल!* %0Aमशीन ने गमरोड को सीधा कमांड भेज दिया है। अपनी प्रोडक्ट लिस्ट चेक करें।" \
          -d parse_mode="Markdown"
