name: 100_Percent_Final_Fix
on:
  workflow_dispatch:

jobs:
  run_machine:
    runs-on: ubuntu-latest
    steps:
      - name: Command to Gumroad
        run: |
          # सीधा कमांड और जवाब रिकॉर्ड करना
          OUTPUT=$(curl -s -X POST https://api.gumroad.com/v2/products \
            -H "Content-Type: application/x-www-form-urlencoded" \
            -d "access_token=${{ secrets.GUMROAD_TOKEN }}" \
            -d "name=AI_FINAL_FIX_$(date +%H%M)" \
            -d "price=500" \
            -d "description=Master Guide" \
            -d "published=true")
          
          echo "Full Response: $OUTPUT"
          
          # टेलीग्राम पर सीधा कच्चा जवाब भेजना (बिना किसी फ़िल्टर के)
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage \
          -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
          -d text="🤖 मशीन रिपोर्ट: $OUTPUT"
